## Rules

- When the user includes questions inside a checklist or review request, answer those questions explicitly and separately instead of only giving a status matrix.
- When the user explicitly narrows an implementation direction, keep the next proposal within that narrowed scope unless asked to expand it.
